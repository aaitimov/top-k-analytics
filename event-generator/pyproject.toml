[project]
name = "event-generator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "confluent-kafka>=2.11.0",
    "numpy>=2.3.2",
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]
